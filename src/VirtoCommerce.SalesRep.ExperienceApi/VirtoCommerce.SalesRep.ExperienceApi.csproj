<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <SonarQubeTestProject>false</SonarQubeTestProject>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="VirtoCommerce.Xapi.Core" Version="3.1012.0" />
    <!-- Consumed directly for IStoreService (store default currency) in SalesRepCustomerOrderStatisticsQueryHandler;
         otherwise only transitive via Xapi.Core. -->
    <PackageReference Include="VirtoCommerce.StoreModule.Core" Version="3.1005.0" />
    <!-- CartModule.Core: the default SalesRepCartFilterRuleResolver uses ModuleConstants.CartType.Wishlist to map the
         built-in "active-carts" kind (non-empty carts that exclude the Wishlist type). Version matches the module.manifest VirtoCommerce.Cart dependency. -->
    <PackageReference Include="VirtoCommerce.CartModule.Core" Version="3.1006.0" />
    <!-- CatalogModule.Core: the default SalesRepTopSellerFilterRuleResolver lists the store catalog's top-level
         categories (ICategorySearchService) as the Top Sellers badges and, on selection, resolves the category to
         the rep's sold products in its subtree via the catalog index (IProductIndexedSearchService).
         Version matches the module.manifest VirtoCommerce.Catalog dependency. -->
    <PackageReference Include="VirtoCommerce.CatalogModule.Core" Version="3.1032.0" />
    <!-- CustomerModule.Core and OrdersModule.Core flow transitively from SalesRep.Core (ProjectReference below). -->
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\VirtoCommerce.SalesRep.Core\VirtoCommerce.SalesRep.Core.csproj" />
  </ItemGroup>
</Project>
