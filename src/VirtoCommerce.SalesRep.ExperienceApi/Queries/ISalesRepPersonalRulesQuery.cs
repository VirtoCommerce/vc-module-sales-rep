namespace VirtoCommerce.SalesRep.ExperienceApi.Queries;

// A rules query whose vocabulary is the caller's own, so it is not narrowed by membership. The counterpart of
// ISalesRepScopedRulesQuery.
public interface ISalesRepPersonalRulesQuery;
